import 'package:flutter/material.dart';


class JsonParsingView extends StatelessWidget {
  const JsonParsingView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
